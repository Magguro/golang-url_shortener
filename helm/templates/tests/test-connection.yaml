apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shortly.fullname" . }}-test-connection"
  labels:
    {{- include "shortly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shortly.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
